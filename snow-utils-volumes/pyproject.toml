[project]
name = "iceberg-external-volume"
version = "0.1.0"
description = "Cortex Code skill for creating Snowflake external volumes with S3"
requires-python = ">=3.12"
dependencies = [
    "snow-utils",
]

[tool.uv.sources]
snow-utils = { git = "https://github.com/kameshsampath/snow-utils" }
