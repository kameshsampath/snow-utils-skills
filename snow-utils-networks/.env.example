# === CONNECTION (captured once, reused by all skills) ===
SNOWFLAKE_DEFAULT_CONNECTION_NAME=
SNOWFLAKE_ACCOUNT=
SNOWFLAKE_USER=
SNOWFLAKE_ACCOUNT_URL=

# === INFRASTRUCTURE (captured once via check_setup.py) ===
SNOW_UTILS_DB=

# === SKILL-SPECIFIC: snow-utils-networks ===
# Admin role for network operations (auto-detected from SA_ADMIN_ROLE if PAT skill was used)
NW_ADMIN_ROLE=

# Network Rule Settings
NW_RULE_NAME=
NW_RULE_DB=
NW_RULE_SCHEMA=NETWORKS

# Optional: Override auto-detected local IP (format: x.x.x.x/32)
LOCAL_IP=
